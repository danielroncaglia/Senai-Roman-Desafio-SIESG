/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PageFirst from './src/pages/login';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PageFirst);
